name: Trigger Listener-Parity OpenSSL Adaptive Build Once

on:
  push:
    branches: [main]

permissions:
  actions: write
  contents: read

jobs:
  dispatch:
    runs-on: ubuntu-latest
    steps:
      - name: Dispatch OpenSSL adaptive workflow
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          gh workflow run openssl-adaptive.yml --repo "$GITHUB_REPOSITORY" --ref main
